# 规则库变更记录

## 维护规则

- 每次修改 `规则库_当前版.json` 前，先确认表格主源已经同步
- 每次修改后，都要追加一条变更记录
- 每次形成可回溯节点后，都要在 `历史版本/` 下保存快照
- 不要删除旧记录，只追加

## 版本记录

### 2026-04-29 v1

- 动作：初始化 JSON 规则库维护结构
- 内容：
  - 新建 `规则库_当前版.json`
  - 新建 `历史版本/`
  - 生成 `历史版本/2026-04-29_v1_初始化模板.json`
  - 新建 `README.md`
  - 新建正式模板和模板说明
- 当前状态：
  - 模板已固化
  - 当前工作版已初始化
  - 尚未开始批量装载 215 条规则

### 2026-04-29 v2

- 动作：从主总表批量装载规则到当前工作版
- 内容：
  - 新建导出脚本 `scripts/export_rules_to_json.py`
  - 新建测试 `tests/test_export_rules_to_json.py`
  - 从 `提取结果/01_当前结果/主总表.xlsx` 的 `最终交付表` 导出 215 条规则
  - 生成历史快照 `历史版本/2026-04-29_v2_215条规则.json`
- 当前状态：
  - `规则库_当前版.json` 已装载 215 条规则
  - 当前工作版状态为 `populated`

### 2026-04-29 v3

- 动作：将当前规则库改为按大类拆分维护
- 内容：
  - 新增 `current/操作规程规则.json`
  - 新增 `current/质量标准规则.json`
  - 新增 `current/流程特征规则.json`
  - 新增 `index.json`
  - 将 `规则库_当前版.json` 调整为自动汇总文件
  - 生成快照 `历史版本/2026-04-29_v3_按大类拆分/`
- 当前状态：
  - 规则库当前按 3 个大类维护
  - 分类数量分别为 132 / 53 / 30
  - 汇总文件仍保留 215 条，作为自动生成总览

### 2026-04-30 v4

- 动作：根据规则追溯核对结果修正规则内容与覆盖说明
- 内容：
  - 修正 `主总表.xlsx` 中 `规则210-215` 的规则内容字段，清理误写入的 JSON 模板残留
  - 同步修正 `终版审计表.xlsx` 与 `提取内容逐项审计.xlsx` 中对应规则正文
  - 修正 `小项覆盖情况` 中 2 处计数说明
  - 重新导出 `current/`、`index.json` 和 `规则库_当前版.json`
  - 生成快照 `历史版本/2026-04-30_v4_追溯修正/`
- 当前状态：
  - `规则210-215` 已恢复为纯自然语言规则表述
  - 覆盖说明与正式总表计数已一致
  - 当前规则总数仍为 215 条

### 2026-04-30 v5

- 动作：修复正式源表中的命名歧义与 `规则210-215` 内容回写异常
- 内容：
  - 在 `主总表.xlsx`、`终版审计表.xlsx`、`提取内容逐项审计.xlsx` 中将 `规则041/042/043`、`规则119/120`、`规则125/212` 完成区分化改名
  - 再次修正 `主总表.xlsx` 中 `规则210-215` 的规则内容字段，清理实际残留的 JSON 拼接与问号损坏
  - 新增 2 项回归测试，校验重复命名区分和 `规则210-215` 正文清洁性
  - 重新导出 `current/`、`index.json` 和 `规则库_当前版.json`
  - 生成快照 `历史版本/2026-04-30_v5_命名澄清与源表修正/`
- 当前状态：
  - 当前规则总数仍为 215 条
  - 正式链路中的重复命名歧义已消除
  - `规则210-215` 已稳定恢复为纯自然语言规则表述
  - `规则163/194` 仍待人工判定

### 2026-05-06 v6

- 动作：完成混合柴油规则口径拆分并固化同源复用判断
- 内容：
  - 在 `主总表.xlsx`、`终版审计表.xlsx`、`提取内容逐项审计.xlsx` 中将 `规则163` 改写为 `混合柴油半成品质量标准规则`
  - 将 `规则194` 改写为 `混合柴油馏出口控制指标规则`，并将其来源页从第 `14` 页修正为第 `22` 页
  - 新增截图引用 `资料019-第22页.png`，同步修正 `证据194` 与 `PDF复核情况` 中的页面摘录
  - 将 `规则092/161`、`规则131/151` 的“同源复用确认”写入正式链路备注
  - 更新 `scripts/validate_rule_library.py`，使已确认的同源复用转为警告而非待人工复判
  - 新增/更新回归测试，并重新导出 `current/`、`index.json` 和 `规则库_当前版.json`
  - 生成快照 `历史版本/2026-05-06_v6_混合柴油口径拆分与同源复用确认/`
- 当前状态：
  - 当前规则总数仍为 215 条
  - 自动核验结构性错误为 0
  - 自动核验待人工复判条目为 0
  - `规则092/161`、`规则131/151` 保留为已确认同源复用

### 2026-05-06 v7

- 动作：补充维护入口约束与历史目录提示
- 内容：
  - 在 `提取结果\新目录入口说明.txt` 中补充当前维护约束和推荐维护顺序
  - 在 `提取结果\90_历史归档\过渡期旧结构_2026-04-29\` 下新增 `00_旧目录勿回写说明.txt`
  - 在 `历史草稿与参考样例\` 下新增 `00_历史草稿勿直接使用.txt`
  - 更新 `历史草稿与参考样例\README.md`，明确旧草稿不再作为正式入口或机器使用入口
  - 新增 `提取结果\00_项目总览\项目维护规范_简版.txt`
- 当前状态：
  - 正式维护入口已进一步明确
  - 旧归档区和历史草稿区已补充显式提示
  - 当前仍以 `current/ + 规则库_当前版.json` 作为正式 JSON 链路
