# 石油项目 Agent 交接说明

更新日期：2026-05-06

## 1. 文档目的

这份文档是后续 agent 的单一入口，目标是让新接手者在最短时间内理解：

- 当前活跃工作目录是什么
- 项目现在进行到哪一步
- 哪些文件是正式口径，哪些只是过程材料
- 当前目录为什么显得复杂
- 后续建议按什么结构继续维护

## 2. 当前项目范围

当前活跃子项目是：

- `提取结果`

这个子项目的核心目标是：

- 从炼化相关 PDF/操作规程中提取证据
- 将证据整理为规则
- 形成可交付的最终规则表
- 对提取结果做逐项审计、二次收敛和 PDF 页面复核

当前周任务重点已经切换为：

- 将已提取规则按统一 JSON 模板整理为结构化规则库

`json规则库\历史草稿与参考样例\` 下另外保留了两个历史/参考 JSON 文件：

- `json规则库\历史草稿与参考样例\refinery_knowledge_rules.json`
- `json规则库\历史草稿与参考样例\规则库.json`

当前正式 JSON 模板位置：

- `json规则库\规则库_JSON模板_正式版.json`
- `json规则库\规则库_JSON模板说明.md`

当前 JSON 规则库维护入口：

- `json规则库\README.md`
- `json规则库\current\`
- `json规则库\index.json`
- `json规则库\规则库_当前版.json`
- `json规则库\规则库_变更记录.md`
- `json规则库\历史版本\`
- `json规则库\历史草稿与参考样例\`

它们不属于 `提取结果` 这套工作簿链路本身，但和“结构化规则表达”相关，后续 agent 不应忽略。

当前项目根目录的正式分工建议按下面理解：

- `提取结果`：正式结果、追溯材料、团队汇报入口
- `json规则库`：结构化规则库与 JSON 模板维护区
- `scripts`、`tests`：当前可复用的导出脚本与测试
- `docs`：计划、参考资料和补充说明
- `延长数据`：原始资料区，不再放早期测试结果
- `90_项目级归档`：项目级历史中间产物与临时脚本归档区

## 3. 当前正式状态

以下状态基于 2026-05-06 时点的现有文件：

- 主总表条目：215
- 规则条目：215
- 最终交付条目：215
- PDF 低置信条目：0
- 待人工确认条目：0
- 终版审计需人工介入条目：0

原始审计状态：

- 原始基本正常：125
- 原始建议抽查：35
- 原始建议复核：55
- 原始需人工处理：0

二次收敛状态：

- 纳入二次收敛总数：90
- 收敛为基本正常：90
- 保留人工判断：0

终版审计状态：

- 终版基本正常：215
- 终版需人工处理：0

## 4. 新 Agent 的默认阅读顺序

如果只是为了快速理解项目，不要先遍历全部目录，按下面顺序看：

1. `提取结果\00_项目总览\当前状态一页纸.md`
2. `提取结果\01_当前结果\主总表.xlsx`
3. `提取结果\01_当前结果\终版审计表.xlsx`
4. `提取结果\02_过程追溯\02_二次收敛\二次收敛结果总表.xlsx`
5. `json规则库\规则库_JSON模板_正式版.json`
6. `json规则库\规则库_JSON模板说明.md`
7. `提取结果\99_交付快照\2026-04-22_第五轮终版\项目状态说明.txt`
8. `提取结果\新目录入口说明.txt`
9. `json规则库\历史草稿与参考样例\refinery_knowledge_rules.json`
10. `json规则库\历史草稿与参考样例\规则库.json`

各文件角色：

- `提取总表-第五轮PDF逐页复核版.xlsx`：当前正式主总表，包含证据、规则、最终交付、覆盖情况、PDF 复核情况
- `提取内容逐项审计-终版.xlsx`：当前审计口径，解释为什么 215 条最终都被判为基本正常
- `二次收敛结果总表.xlsx`：解释原始抽查/复核问题如何被归类并收敛
- `00_项目总览\当前状态一页纸.md`：当前面向人和 agent 的最快入口，也是团队汇报时的总览页
- `json规则库\规则库_JSON模板_正式版.json`：当前已固化的正式 JSON 模板，后续批量整理规则应以此为准
- `json规则库\current\`：当前 JSON 规则库按大类拆分后的维护入口
- `json规则库\index.json`：当前分类索引，记录 3 个大类文件与规则数量
- `json规则库\规则库_当前版.json`：当前 JSON 规则库自动汇总文件，汇总 3 个大类共 215 条规则
- `json规则库\规则库_变更记录.md`：当前 JSON 规则库版本变更记录
- `99_交付快照`：当前对外发送用的整包副本目录，不应再被视为唯一主版本
- `json规则库\历史草稿与参考样例\refinery_knowledge_rules.json`：看起来是更偏建模/规划规则的结构化规则样例，含工业公理、优化目标、装置规则
- `json规则库\历史草稿与参考样例\规则库.json`：更像 JSON 规则表达模板或草稿，但当前存在编码异常或内容不完整问题，不能直接作为稳定机器输入

## 5. 当前目录结构的主要问题

当前目录并不是乱，而是已经进入“结果越来越多后开始冗余”的阶段。主要问题有四个：

- `总表` 和 `终版交付包` 同时保存了多份正式文件，存在双主版本风险
- 说明文件分散在根目录、总表目录、归档目录、人工确认目录，信息有重复
- 页面截图同时按“轮次”和“规则类别”两种方式组织，逻辑不统一
- 历史归档、当前结果、交付副本三套东西边界不够硬，后续更新时容易漂移

这意味着当前结构短期能用，但不适合长期滚动维护。

## 6. 推荐的长期目录结构

建议后续逐步迁移到下面这套结构，不建议一次性大搬家：

```text
提取结果/
  00_项目总览/
    当前状态一页纸.md
    周报归档/
    汇报附件/

  01_当前结果/
    主总表.xlsx
    终版审计表.xlsx
    项目状态说明.txt

  02_过程追溯/
    01_原始审计/
    02_二次收敛/
    03_结构样例/
    04_覆盖检查/
    05_规则追溯核对/

  03_人工确认/
    需人工确认汇总.xlsx
    来源PDF/
    截图/

  04_截图追溯/
    01_第五轮复核截图/
    02_第四轮复核截图/
    03_汇总引用截图/

  90_历史归档/

  99_交付快照/
    2026-04-22_第五轮终版/
```

这套结构的核心原则：

- `01_当前结果` 只保留唯一正式版本
- `00_项目总览` 只负责汇报入口和状态摘要，不存主数据
- `02_过程追溯` 只放推导过程，不放主版本
- `03_人工确认` 是当前待办队列，不和历史结果混放
- `04_截图追溯` 统一按用途组织，不再混用“分类截图”和“轮次截图”
- `90_历史归档` 只进不出
- `99_交付快照` 只在对外发送时生成，生成后不回写

## 6.1 第一阶段迁移状态

截至 2026-04-29，第一阶段目录重组已经完成以下事项：

- 已建立 `00_项目总览`、`01_当前结果`、`02_过程追溯`、`03_人工确认`、`04_截图追溯`、`99_交付快照`
- 已将当前正式主总表复制为 `01_当前结果\主总表.xlsx`
- 已将当前正式终版审计表复制为 `01_当前结果\终版审计表.xlsx`
- 已将原 `终版交付包` 完整复制为 `99_交付快照\2026-04-22_第五轮终版`
- 已建立团队周汇报模板 `00_项目总览\周报归档\周报模板.md`
- 已将旧入口目录及旧根目录说明文件统一移动到 `90_历史归档\过渡期旧结构_2026-04-29`
- 已完成项目根目录第一阶段整理：`tmp`、`output` 和 `延长数据\规则提取结果_Test` 已移入 `90_项目级归档`
- 已将根目录散落的参考材料移入 `docs\参考资料\`
- 已将历史 JSON 草稿和参考样例移入 `json规则库\历史草稿与参考样例\`
- 已完成项目根目录第二阶段精简：归档区脚本路径已改为本地相对/当前路径口径，`延长数据` 已补充入口说明
- 已将 `延长数据\规则库.json` 也并入 `json规则库\历史草稿与参考样例\`，避免原始资料区混入规则草稿
- 已新增 `02_过程追溯\05_规则追溯核对\`，保存 2026-04-30 的规则追溯核对结果
- 已在 `02_过程追溯\05_规则追溯核对\` 下补充重复规则口径判定清单，用于后续去重或改名决策
- 已完成一轮不改计数的重复规则改名落地：`规则041/042/043`、`规则119/120`、`规则125/212` 已在主总表与两份审计表中完成区分化命名
- 已重新修正 `主总表.xlsx` 中 `规则210-215` 的规则内容字段，清理实际残留的 JSON 拼接与问号损坏，并重新导出 JSON 规则库
- 已新增 2 项回归测试，锁定重复命名区分和 `规则210-215` 自然语言正文清洁性
- 已完成 `规则163/194` 的正式口径拆分：`规则163` 保留为“混合柴油半成品质量标准规则”，`规则194` 改写为“混合柴油馏出口控制指标规则”，并同步修正证据页、审计表和 JSON 导出
- 已将 `规则092/161`、`规则131/151` 的“同源复用”决定正式写回主链路备注，并更新自动核验逻辑；当前自动核验不再存在待人工复判条目

本阶段尚未做的事情：

- 未处理 `规则库.json` 的编码或合法性问题
- 未对归档区内旧结构做二次精简

## 7. 当前文件到目标结构的迁移映射

| 当前路径 | 目标路径 | 迁移建议 | 说明 |
| --- | --- | --- | --- |
| `总表\当前主文件\提取总表-第五轮PDF逐页复核版.xlsx` | `01_当前结果\主总表.xlsx` | 重命名为稳定主文件名 | 当前唯一正式主总表 |
| `总表\当前主文件\提取内容逐项审计-终版.xlsx` | `01_当前结果\终版审计表.xlsx` | 重命名为稳定主文件名 | 当前唯一正式审计口径 |
| `总表\当前主文件\提取内容逐项审计-终版摘要.txt` | `01_当前结果\项目状态说明.txt` 或 `02_过程追溯\02_二次收敛\` | 合并或保留为补充摘要 | 取决于是否保留独立摘要 |
| `总表\过程分析文件\提取内容逐项审计.xlsx` | `02_过程追溯\01_原始审计\提取内容逐项审计.xlsx` | 直接迁移 | 原始审计输入 |
| `总表\过程分析文件\提取内容逐项审计-摘要.txt` | `02_过程追溯\01_原始审计\提取内容逐项审计-摘要.txt` | 直接迁移 | 原始审计摘要 |
| `总表\过程分析文件\二次收敛结果总表.xlsx` | `02_过程追溯\02_二次收敛\二次收敛结果总表.xlsx` | 直接迁移 | 审计收敛依据 |
| `总表\过程分析文件\二次收敛结果摘要.txt` | `02_过程追溯\02_二次收敛\二次收敛结果摘要.txt` | 直接迁移 | 审计收敛摘要 |
| `总表\过程分析文件\仅需人工判断清单.xlsx` | `03_人工确认\仅需人工判断清单.xlsx` | 直接迁移 | 当前为 0 条，但应保留为待办入口 |
| `需人工确认\需人工确认汇总.xlsx` | `03_人工确认\需人工确认汇总.xlsx` | 直接迁移 | 当前人工确认主表 |
| `需人工确认\来源PDF\*` | `03_人工确认\来源PDF\*` | 直接迁移 | 待确认来源材料 |
| `需人工确认\截图\*` | `03_人工确认\截图\*` | 直接迁移 | 待确认截图 |
| `总表\过程分析文件\提取总表-第五轮PDF逐页复核版-结构样例.xlsx` | `02_过程追溯\03_结构样例\结构样例.xlsx` | 迁移并简化命名 | 用于理解结构，不是主结果 |
| `总表\过程分析文件\提取总表-第五轮PDF逐页复核版-结构样例-含JSON格式.xlsx` | `02_过程追溯\03_结构样例\结构样例-含JSON.xlsx` | 迁移并简化命名 | 用于展示自然语言+JSON 口径 |
| `总表\过程分析文件\提取总表-第五轮PDF逐页复核版-结构样例-说明.txt` | `02_过程追溯\03_结构样例\结构样例-说明.txt` | 直接迁移 | 结构说明 |
| `总表\过程分析文件\提取总表-第五轮PDF逐页复核版-结构样例-含JSON格式-说明.txt` | `02_过程追溯\03_结构样例\结构样例-含JSON-说明.txt` | 直接迁移 | JSON 结构说明 |
| `总表\过程分析文件\小项覆盖情况说明.txt` | `02_过程追溯\04_覆盖检查\小项覆盖情况说明.txt` | 直接迁移 | 覆盖检查材料 |
| `页面截图\第五轮截图\*` | `04_截图追溯\01_第五轮复核截图\*` | 直接迁移 | 当前最重要截图集 |
| `页面截图\第四轮截图\*` | `04_截图追溯\02_第四轮复核截图\*` | 直接迁移 | 历史复核截图 |
| `页面截图\汇总截图\*` | `04_截图追溯\03_汇总引用截图\*` | 直接迁移 | 汇总引用材料 |
| `页面截图\操作规程规则\*`、`页面截图\流程特征规则\*`、`页面截图\质量标准规则\*` | `90_历史归档\旧截图分类\*` | 建议归档 | 当前不再是主组织方式 |
| `终版交付包\*` | `99_交付快照\2026-04-22_第五轮终版\*` | 作为交付快照保留 | 不再当作“当前主目录”继续维护 |
| `历史版本归档\*` | `90_历史归档\*` | 保持归档定位 | 历史结果只读化 |

## 8. 建议的维护规则

后续如果要长期维护，建议只执行下面几条硬规则：

1. 任何时刻只能有一份“当前正式版本”，只放在 `01_当前结果`
2. `99_交付快照` 只在对外发送时生成，生成后不再修改
3. 所有审计、二次收敛、结构样例、覆盖检查一律进入 `02_过程追溯`
4. 人工确认相关内容一律进入 `03_人工确认`
5. 新增截图必须先判断用途，再放进 `04_截图追溯` 的对应子目录
6. 历史结果不回写，不和当前目录混放
7. 每次状态发生变化后，至少更新这 3 处：
   - 本文档
   - 当前结果中的状态说明文件
   - 对应主表或审计表中的统计结果

## 9. 当前已知风险

- 过渡归档区仍保留完整旧结构，虽然不再作为主入口，但后续若有人绕过新入口直接修改，仍可能造成口径漂移
- `规则库.json` 当前显示存在中文乱码或内容不完整问题，后续若要机器消费，需先确认编码和 JSON 合法性
- JSON 规则库已切换为按大类维护，后续若继续拆到小项层级，需要额外控制文件碎片化风险

## 10. 建议的下一步

如果后续要继续整理，优先顺序建议如下：

1. 用 `00_项目总览` 和 `01_当前结果` 跑通一次团队周汇报
2. 在一个完整使用周期内验证新入口是否足够清晰
3. 确认后续所有新增文件优先进入新目录，而不是继续落在归档区旧结构中
4. 条件成熟后，再决定是否删除过渡归档区中不再需要的副本
5. 单独处理 `规则库.json` 的编码和结构合法性问题

## 11. 给下一位 Agent 的一句话总结

这是一个已经完成五轮提取和 PDF 逐页复核的规则抽取项目；当前正式口径是 215 条全部通过终版审计，目录的主要问题不是结果不清楚，而是“正式结果”和“交付副本”并存导致维护成本偏高。后续工作应优先做目录单源化，而不是重新审计内容本身。
