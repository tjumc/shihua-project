# 当前状态一页纸

更新日期：2026-05-06

## 项目目标

从炼化相关 PDF/操作规程中提取证据，整理为结构化规则，形成可交付规则表，并通过逐项审计、二次收敛和页面复核保证结果可追溯。

## 当前结论

- 当前正式主总表：`01_当前结果/主总表.xlsx`
- 当前正式审计表：`01_当前结果/终版审计表.xlsx`
- 当前规则追溯入口：`02_过程追溯/05_规则追溯核对/`
- 当前可对外发送快照：`99_交付快照/2026-04-22_第五轮终版/`
- 当前 JSON 规则库维护入口：`../json规则库/current/`
- 当前 JSON 规则库自动汇总：`../json规则库/规则库_当前版.json`
- `规则041/042/043`、`规则119/120`、`规则125/212` 已完成区分化命名，正式链路不再存在同名歧义
- `规则210-215` 已恢复为纯自然语言规则表述，并已完成 JSON 重导与回归测试
- `规则163/194` 已完成口径拆分：前者保留为“混合柴油半成品质量标准规则”，后者改写为“混合柴油馏出口控制指标规则”
- `规则092/161`、`规则131/151` 已确认按“同源复用”保留双口径，并已写入正式链路备注与自动核验规则

## 当前统计

- 主总表条目：215
- 规则条目：215
- 最终交付条目：215
- 原始建议抽查：35
- 原始建议复核：55
- 二次收敛处理：90
- 终版需人工介入：0
- 当前待人工确认：0

## 本阶段新增进展

- 已完成 `02_过程追溯/05_规则追溯核对/` 下的重复规则口径判定整理
- 已完成一轮“不改数量、先改命名”的正式回写，并同步到 `终版审计表.xlsx` 与 `提取内容逐项审计.xlsx`
- 已修复 `规则210-215` 在正式源表中的 JSON 残留与问号损坏问题
- 已新增回归测试，校验重复命名区分和 `规则210-215` 正文清洁性
- 已完成 `规则163/194` 的证据页、规则名称、规则正文与 JSON 导出同步修正
- 已将 `规则092/161`、`规则131/151` 的“同源复用”决定正式写入，并更新自动核验报告口径

## 当前主要风险

- 过渡归档区仍保留完整旧结构，团队需要避免继续从旧路径回写
- `规则库.json` 历史草稿仍存在编码或内容完整性问题，尚未纳入本阶段处理
- 薄覆盖小项仍建议继续做遗漏复核，但这已不是当前正式链路的阻断项

## 当前优先事项

- 继续以 `json规则库/current/` 作为按大类维护入口，`规则库_当前版.json` 仅作为自动汇总文件
- 对薄覆盖小项继续做遗漏复核，优先补弱项而不是继续扩写高覆盖部分
- 推进 `docs/技术报告/` 下的技术报告正文扩写
- 后续新增文件继续优先进入新目录结构，不回写旧入口

## 默认阅读顺序

1. 本文件
2. `01_当前结果/主总表.xlsx`
3. `01_当前结果/终版审计表.xlsx`
4. `02_过程追溯/05_规则追溯核对/2026-04-30_规则追溯核对结论.md`
5. `02_过程追溯/05_规则追溯核对/2026-04-30_重复规则口径判定结论.md`
