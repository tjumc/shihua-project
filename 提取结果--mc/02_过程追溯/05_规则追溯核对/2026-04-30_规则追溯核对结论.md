# 2026-04-30 规则追溯核对结论

## 1. 核对范围

本轮核对基于以下正式文件：

- `01_当前结果/主总表.xlsx`
- `01_当前结果/终版审计表.xlsx`
- `02_过程追溯/04_覆盖检查/小项覆盖情况说明.txt`
- `延长数据/` 下原始 PDF

同时生成了配套核对表：

- `02_过程追溯/05_规则追溯核对/2026-04-30_规则追溯核对表.xlsx`

## 2. 核对结论

当前 `215` 条规则在正式链路上总体是闭环的：

- `215/215` 条规则都在 `最终交付表` 中
- `215/215` 条规则都能在 `终版审计明细` 中找到
- `215/215` 条规则的终版审计状态均为 `基本正常`
- 当前未发现来源文件丢失
- 当前未发现规则编号断号

在本轮核对后，以下两类问题已经完成修正：

- `规则210-215` 的规则内容字段已在正式源表中恢复为纯自然语言，并已重新导出 JSON 规则库
- `小项覆盖情况说明` 和 `小项覆盖情况` sheet 中 2 处计数不一致已同步修正
- `规则041/042/043`、`规则119/120`、`规则125/212` 已完成区分化改名并同步到审计链路

修正后，当前剩余的主要风险不再是“明显错误”，而是口径层面的重复和薄覆盖小项。

## 3. 发现的问题

### 3.1 已修正的问题

- `规则210-215` 的规则内容字段已恢复为纯自然语言规则表述，历史残留的 JSON 拼接与问号损坏已清理
- `规则041/042/043`、`规则119/120`、`规则125/212` 已完成区分化命名，正式链路中的同名歧义已消除
- `操作规程规则 / 设备启停、切换、巡检、维护操作要求` 的覆盖说明已修正为 `12` 条
- `操作规程规则 / 安全环保、防静电、防泄漏、防超温超压强制条款` 的覆盖说明已修正为 `10` 条

### 3.2 当前剩余风险：存在跨大类的重复语义规则

发现两组明显重叠：

- `规则092` 与 `规则161`
  同一句“进口原油反输时，应在原油储罐内化验合格后...”
  但分别落在：
  - `操作规程规则 / 物料收付、调合、输送安全操作`
  - `流程特征规则 / 装置间物料平衡`

- `规则131` 与 `规则151`
  同一句“成品油调和完毕后应静置、脱水并分析合格...”
  但分别落在：
  - `操作规程规则 / 采样、分析、质量控制操作规范`
  - `流程特征规则 / 上下游协同逻辑`

判断：

- 这不一定是错，也可能是项目允许“一条原文按不同规则视角重复建模”
- 但如果后续需要唯一规则口径，这两组应先定规则，避免重复计数

### 3.3 当前剩余风险：薄覆盖小项仍建议二次回看

这些小项当前条数较少，虽然不一定有问题，但更容易漏项：

- `流程特征规则 / 单元间物料平衡`：1 条
- `流程特征规则 / 能量耦合`：1 条
- `操作规程规则 / 停工操作步骤`：3 条
- `质量标准规则 / 原油质量标准`：5 条
- `操作规程规则 / 开工操作步骤`：5 条

## 4. 当前未发现的问题

- 未发现来源文件缺失
- 未发现规则编号缺失
- 未发现空白必填字段
- 未发现 PDF 复核缺失条目
- 未发现终版审计异常条目
- 未再发现规则内容中的 JSON 模板残留
- 未再发现 `小项覆盖情况` 与正式总表计数不一致

## 5. 建议的处理顺序

1. 先对 `规则163 / 规则194` 做一次人工复判，明确是否存在重复计数
2. 再决定跨大类重复语义规则是否长期允许保留双口径，并是否补充“同源复用”标记
3. 最后对薄覆盖小项做一轮遗漏复核；若后续继续扩充规则库，优先补薄覆盖小项而不是继续扩写已高覆盖部分
